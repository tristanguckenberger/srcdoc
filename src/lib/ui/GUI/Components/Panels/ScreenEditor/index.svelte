<script>
	/**
	 * Panel controling the current selected screens options,
	 * details, logic, connections, etc.
	 * Similar to object logic, should create a game class
	 * and game loop for the screen if the type is [LVL]
	 * Otherwise if [UI] type, no need for game loop but we
	 * can add other stuff that allows users to create
	 * complex UIs
	 *
	 */
</script>
